{
  "name": "hearing-test",
  "scripts": {
      "dev": "esbuild ./src/App.tsx --minify --bundle --outfile=./serve/js/bundle.js --loader:.png=dataurl --watch --sourcemap",
    "build": "esbuild ./src/App.tsx --minify --bundle --outfile=./serve/js/bundle.js --loader:.png=dataurl"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "styled-components": "^5.3.3"
  },
  "devDependencies": {
    "esbuild": "^0.14.25"
  }
}
